# Contributing

## Workflow
- Create a `feature/<short-description>` branch off `dev`.
- Use pull requests to merge into `dev`, then `main`.

## Commit format
type(scope): short description

Examples:
- feat(asm): add texture metrics to classifier2
- fix(turb): correct turbidity threshold logic
- docs(literature): add ASM review summary
